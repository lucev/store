<%= form_for(@variant, :url => @path) do |f| %>
  <% if @variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variant.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
      <% @variant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.fields_for :product do |v| %>
    <div class="field">
      <%= v.label :name %><br />
      <%= v.text_field :name %>
    </div>
    <div class="field">
      <%= v.label :description %><br />
      <%= v.text_field :description %>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :sku %><br />
    <%= f.text_field :sku %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
