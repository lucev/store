<%= form_for(@variant, :url => @path,
      :role => "form") do |f| %>
  <% if @variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variant.errors.count, "error") %> prohibited this variant from being saved:</h2>

      <ul>
      <% @variant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sku %><br />
    <%= f.text_field :sku %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit t(:save), class: 'btn btn-primary' %>
    <%= link_to t(:cancel), admin_product_variants_path(I18n.locale, @master_variant), class: 'btn btn-link' %>
  </div>
<% end %>
