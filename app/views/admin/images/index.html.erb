<h1>Listing images for product</h1>

<p>Name: <%= @master_variant.product.name %></p>
<p>Description: <%= @master_variant.product.description %>

<table class="table table-striped table-hover">
  <tr>
    <th>Sku</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @variants.each do |variant| %>
  <tr>
    <td><%= variant.sku %></td>
    <td><%= link_to 'Show', admin_product_variant_path(variant.master_id, variant) %></td>
    <td><%#= link_to 'Edit', edit_product_variant_path(variant) %></td>
    <td><%#= link_to 'Destroy', product_variant_path(variant), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New Variant', new_variant_path %>
