<h1>Listing products</h1>

<table class="table table-striped table-hover">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @master_variants.each do |variant| %>
  <tr>
    <td><%= variant.product.name %></td>
    <td><%= variant.product.description %></td>
    <td><%= link_to 'Show', product_path(I18n.locale, variant) %></td>
  </tr>
<% end %>
</table>